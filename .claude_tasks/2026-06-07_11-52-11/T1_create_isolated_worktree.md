# Task 1: Create isolated worktree

## Description

Create a scoped branch and sibling worktree from `origin/main` so the OpenAI MCP video work does not touch the existing dirty checkout.

## Decisions

- Branch: `codex/openai-mcp-video`.
- Worktree: `/Users/petereilly2021/Projects/barrelbook-website-openai-mcp-video`.
- Existing untracked `ads/` content in the original checkout is intentionally left untouched.

## Code Snippets

No implementation code.

